# Copy this file as application-secrets.properties and insert the local token.
# application-secrets.properties is ignored by Git and must never be committed.
CARDTRADER_API_TOKEN_FILE_VALUE=replace-with-your-cardtrader-token
